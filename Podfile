 # Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'scope-ios-cocoapods-starter' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
   use_frameworks!
   pod 'Alamofire', '~> 4.9'
   pod 'ScopeAgent'

  # Pods for scope-ios-cocoapods-starter

  target 'scope-ios-cocoapods-starterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'scope-ios-cocoapods-starterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
